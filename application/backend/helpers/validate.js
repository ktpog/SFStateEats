// const validateImageExtension = validExtensions => {
//   if (stat.size > 2000) {
//     return res.status(400).send({ error: "Images should be less than 2mb in size" });
//   }
// };
// const validateImageSize = (currentSizeInKb, maxSizeInKb) => {
//   if (currentSizeInKb > maxSizeInKb) {
//     return { error: `Images should be less than ${maxSizeInKb}kb in size` };
//   } else {
//     return true;
//   }
// };

// module.exports = {
//   validateImageExtension,
//   validateImageSize
// };
